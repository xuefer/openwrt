--- eglibc-2.13-r10495/libc/manual//Makefile	2010-04-01 03:12:26.000000000 +0800
+++ eglibc-2.13-r10495/libc/manual//Makefile.new	2011-11-13 02:16:03.000000000 +0800
@@ -243,9 +243,12 @@
 .PHONY: stubs
 stubs: $(objpfx)stubs
 endif
-$(objpfx)stubs ../po/manual.pot $(objpfx)stamp%:
+$(objpfx)stamp%:
 	$(make-target-directory)
 	touch $@
+$(objpfx)stubs ../po/manual.pot:
+	$(make-target-directory)
+	touch $@
 
 # Make the target directory if it doesn't exist, using the `mkinstalldirs'
 # script that does `mkdir -p' even if `mkdir' doesn't support that flag.
--- eglibc-2.13-r10495/libc/manual//Makefile	2010-04-01 03:12:26.000000000 +0800
+++ eglibc-2.13-r10495/libc/manual//Makefile.new	2011-11-13 02:16:03.000000000 +0800
@@ -243,9 +243,12 @@
 .PHONY: stubs
 stubs: $(objpfx)stubs
 endif
-$(objpfx)stubs ../po/manual.pot $(objpfx)stamp%:
+$(objpfx)stamp%:
 	$(make-target-directory)
 	touch $@
+$(objpfx)stubs ../po/manual.pot:
+	$(make-target-directory)
+	touch $@
 
 # Make the target directory if it doesn't exist, using the `mkinstalldirs'
 # script that does `mkdir -p' even if `mkdir' doesn't support that flag.
